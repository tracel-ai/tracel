[package]
name = "tracel"
repository = "https://github.com/tracel-ai/tracel"
edition.workspace = true
version.workspace = true
readme.workspace = true

[features]
heat-sdk = ["dep:heat-sdk"]
heat-cli = ["dep:heat-cli"]

[dependencies]
heat-sdk = { path = "../heat-sdk", version = "0.1.0", optional = true }
heat-cli = { path = "../heat-cli", version = "0.1.0", optional = true }