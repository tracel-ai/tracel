[package]
name = "guide-shell"
edition.workspace = true
version.workspace = true
readme.workspace = true
license.workspace = true

[dependencies]
tracel = { path = "../../crates/tracel", version = "0.1.0", features = [
    "heat-sdk",
    "heat-sdk-cli",
] }
burn = { workspace = true, features = ["vision", "train"] }
heatcli = { path = "../heatcli", version = "0.1.0" }
anyhow = "1.0.53"
clap = { version = "4.4.18", features = ["derive"] }
