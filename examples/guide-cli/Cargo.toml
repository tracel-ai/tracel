[package]
edition.workspace = true
name = "guide-cli"
publish = false
version.workspace = true

[dependencies]
burn = { workspace = true, features = ["vision", "train"] }
tracel = { path = "../../crates/tracel", version = "0.1.0", features = ["heat-sdk", "heat-sdk-cli"] }
